import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const HealthDataPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>💚 健康数据</Title>
      <p>健康数据模块开发中...</p>
    </div>
  );
};

export default HealthDataPage;